# Contributing

Contributions are welcome. Please keep changes generic and portable:

- no hardcoded user names, home directories, machine names, or credentials;
- no committed runtime state, logs, browser data, or local memory files;
- prefer environment variables for local paths and endpoints;
- run `python -m compileall .` before submitting Python changes.
